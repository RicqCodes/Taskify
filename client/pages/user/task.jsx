import { getAppLayout } from "@/components/common/layouts/private";
import React from "react";

const Task = () => {
  return <div>task</div>;
};

export default Task;

Task.getLayout = (pages) => getAppLayout(pages);

import { getAppLayout } from "@/components/common/layouts/private";
import React from "react";

const Settings = () => {
  return <div>settings</div>;
};

export default Settings;

Settings.getLayout = (page) => getAppLayout(page);
